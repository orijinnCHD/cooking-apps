import React from 'react';

const SearchBar = () => {
    return (
        <form action="">
            <input type="text" placeholder='Placer le texte ici' />
        </form>
    );
};

export default SearchBar;